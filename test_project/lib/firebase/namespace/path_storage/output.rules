rules_version = '2';
service firebase.storage {
  match /databases/{database}/documents {
    function test(other) {
      let a = /databases/(default)/documents/path/to/resource;
      return true;
    }
  }
}
